name: Lint

on:
  pull_request:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Lint
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/setup@v2
      - name: Format Check
        run: pnpm fmt:check
      - name: Lint
        run: pnpm lint
